# Comprehensive Plan: Complete Stub Elimination & Full Non-Euclidean Implementation Audit

**Goal:** Thoroughly audit and code out 100% of stubbed code, placeholders, and simplified shortcuts across `test/geomind/` (including `streams.car`, `moe.car`, `e8_attention_engine.car`) before proceeding to any new sprint.  
**Date:** August 11, 2026  
**Archive File:** `docs/archive/stub_elimination_sprint_plan.md`  

---

## I. Audit Findings & Targets

1. **`test/geomind/streams.car`**:
   - **`SpectralStream`**: Implement real DFT spectral frequency filtering.
   - **`PoincareStream`**: Implement Poincaré hyperbolic metric distance ($d_P(u,v) = \text{acosh}(z)$).
   - **`HomologyStream`**: Implement 3-node simplicial complex persistence loop density.
   - **`EikonalStream`**: Implement geodesic optical path ray-tracing.
   - **`HeatKernelStream`**: Implement discrete graph Laplacian heat diffusion ($Y = X - t Z + \frac{1}{2} t^2 D Z$).
   - **`TrialityStream`**: Implement symplectic 3-block cyclic rotation ($SU(3)^3$).

2. **`test/geomind/moe.car`**:
   - Implement full $4 \times 4$ Freudenthal division algebra expert matrix ($\mathbb{R}, \mathbb{C}, \mathbb{H}, \mathbb{O}$) and Sasaki tangent bundle phase-space router ($TM = M \times T_x M$).

3. **`test/geomind/e8_attention_engine.car`**:
   - Implement $E_8$ Lie algebra 240-root lattice projection (`geom_e8_root_coordinate`), query/key/value projection, and `std::autotune` micro-kernel tiling.

---

## II. Execution & Verification Steps
1. Edit `test/geomind/streams.car`, `moe.car`, and `e8_attention_engine.car` to replace placeholders with exact mathematical implementations.
2. Rebuild `cartanc.exe` release compiler binary and native `geomind.exe`.
3. Verify clean execution of `geomind.exe --help`, `geomind.exe --chat`, and `geomind.exe --train-sft` with exit status 0.
